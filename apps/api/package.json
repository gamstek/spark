{
  "name": "@spark/api",
  "type": "module",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "predev": "pnpm db:migrate",
    "dev": "node --env-file-if-exists=../../.env --watch --import tsx src/main.ts",
    "start": "node dist/src/main.js",
    "prebuild": "pnpm --filter @spark/templates build && pnpm --filter @spark/contracts build",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @spark/templates build && pnpm --filter @spark/contracts build",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false && tsc -p tsconfig.test.json --pretty false",
    "lint": "eslint .",
    "test": "vitest run --config vitest.unit.config.ts --passWithNoTests",
    "pretest:integration": "pnpm --filter @spark/templates build && pnpm --filter @spark/contracts build",
    "test:integration": "vitest run --config vitest.config.ts",
    "db:migrate": "node --env-file-if-exists=../../.env --import tsx database/migrate.ts",
    "admin:create": "node --env-file-if-exists=../../.env --import tsx database/seeds/create-admin.ts",
    "pretemplates:check": "pnpm --filter @spark/templates build",
    "templates:check": "node --env-file-if-exists=../../.env --import tsx scripts/check-template-compatibility.ts"
  },
  "dependencies": {
    "@nestjs/common": "12.0.1",
    "@nestjs/core": "12.0.1",
    "@nestjs/platform-fastify": "12.0.1",
    "@spark/contracts": "workspace:*",
    "@spark/templates": "workspace:*",
    "exceljs": "^4.4.0",
    "fastify": "5.12.3",
    "pg": "8.23.0",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.2",
    "typeorm": "1.1.1",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "tsx": "4.23.13",
    "typescript": "6.0.3",
    "vitest": "5.0.0"
  }
}
